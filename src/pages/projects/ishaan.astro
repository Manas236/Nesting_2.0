---
import BaseLayout from "../../layouts/BaseLayout.astro";
import SiteHeader from "../../components/SiteHeader.astro";
import QrBlock from "../../components/QrBlock.astro";
import ProjectGallery from "../../components/ProjectGallery.astro";
import AreaStats from "../../components/AreaStats.astro";
import BuildProgress from "../../components/BuildProgress.astro";
import { brand, contact, features, projects } from "../../data/site";
import {
  TBD,
  ishaan,
  sales,
  heroStats,
  overview,
  unitTypes,
  unitTypesNote,
  buildProgress,
  floorPlans,
  levels,
  amenityGroups,
  amenitiesNeeded,
  developerNotes,
  whyIshaan,
  gallery,
} from "../../data/ishaan";

/* ---------- Line-icon set (24×24, stroke currentColor) ----------
   Keyed to the `icon` fields in src/data/ishaan.ts. Kept here as
   presentation, the way the homepage keeps its amenity icons. The
   set carries the full Building / Lifestyle / Interiors vocabulary
   used by shikhar.ts and dhruva.ts, so filling `amenityGroups` in
   the data file needs no change here.                             */
const icons: Record<string, string> = {
  tower: `<rect x="7" y="3" width="10" height="18" rx="1"/><path d="M4 21h16"/><path d="M10.5 7h.01M13.5 7h.01M10.5 11h.01M13.5 11h.01M10.5 15h.01M13.5 15h.01"/>`,
  elevator: `<rect x="5" y="2.5" width="14" height="19" rx="2"/><path d="M12 2.5v19"/><path d="m9.6 8.5-1.1 1.6h2.2z"/><path d="m9.6 15.5-1.1-1.6h2.2z"/><path d="M14.5 9h2.5M14.5 12h2.5M14.5 15h2.5"/>`,
  lobby: `<path d="M6 21V5a2 2 0 0 1 2-2h8a2 2 0 0 1 2 2v16"/><path d="M4 21h16"/><path d="M14.2 12h.01"/>`,
  stairs: `<path d="M3 21h4v-4h4v-4h4v-4h4v-4h2"/><path d="M3 21v-2"/>`,
  parking: `<rect x="3.5" y="3.5" width="17" height="17" rx="3"/><path d="M9.5 16.5v-9h3.4a2.8 2.8 0 0 1 0 5.6H9.5"/>`,
  waterproof: `<path d="M12 3 5 6v5c0 4.6 3 8.1 7 9 4-.9 7-4.4 7-9V6z"/><path d="M12 9.5c1.2 1.6 2 2.7 2 3.7a2 2 0 0 1-4 0c0-1 .8-2.1 2-3.7z"/>`,
  gym: `<path d="M6.5 8v8M3.5 9.8v4.4M17.5 8v8M20.5 9.8v4.4M6.5 12h11"/>`,
  rooftop: `<circle cx="12" cy="12.5" r="3.2"/><path d="M12 4.5v2M5 12.5H3.2M20.8 12.5H19M6.9 7.4 5.7 6.2M18.3 6.2l-1.2 1.2"/><path d="M3 20.5h18"/>`,
  shop: `<path d="M3.5 9 5 5h14l1.5 4z"/><path d="M5 9.5V20h14V9.5"/><path d="M9.5 20v-5.5h5V20"/>`,
  star: `<path d="m12 4 2.3 5 5.4.5-4.1 3.6 1.3 5.3L12 16.6 7.1 18.4l1.3-5.3-4.1-3.6 5.4-.5z"/>`,
  tiles: `<rect x="3.5" y="3.5" width="17" height="17" rx="1"/><path d="M3.5 12h17M12 3.5v17"/>`,
  faucet: `<path d="M12 3.5c2.6 3.7 4.2 6.2 4.2 8.3a4.2 4.2 0 0 1-8.4 0c0-2.1 1.6-4.6 4.2-8.3z"/>`,
  window: `<rect x="4" y="3" width="16" height="18" rx="1"/><path d="M12 3v18M4 12h16"/>`,
  grill: `<rect x="4" y="4" width="16" height="16" rx="1"/><path d="M9 4v16M14 4v16M4 9h16M4 14h16"/>`,
  fan: `<circle cx="12" cy="12" r="1.8"/><path d="M12 10.2c-.4-2.8.6-5.2 2.4-5.2 1.5 0 1.8 2.3-.6 3.6M13.8 12c2.8-.4 5.2.6 5.2 2.4 0 1.5-2.3 1.8-3.6-.6M12 13.8c.4 2.8-.6 5.2-2.4 5.2-1.5 0-1.8-2.3.6-3.6M10.2 12c-2.8.4-5.2-.6-5.2-2.4 0-1.5 2.3-1.8 3.6.6"/>`,
  loft: `<rect x="4" y="4" width="16" height="16" rx="1"/><path d="M4 9h16"/><path d="M8 6.4h.01M11 6.4h.01"/><path d="M11 13h6"/>`,
};

const arrow = `<path d="M5 12h14M13 6l6 6-6 6"/>`;
const pin = `<path d="M12 21s7-7.5 7-12a7 7 0 1 0-14 0c0 4.5 7 12 7 12Z"/><circle cx="12" cy="9" r="2.2"/>`;
const check = `<path d="m5 12.5 4 4 10-10"/>`;

const sections = [
  { label: "Overview", href: "#overview" },
  { label: "Units", href: "#units" },
  { label: "Progress", href: "#progress" },
  { label: "Floor plans", href: "#floorplans" },
  { label: "Amenities", href: "#amenities" },
  { label: "Gallery", href: "#gallery" },
  { label: "About", href: "#developer" },
];

/* A value we were never given renders muted and italic, so a gap
   always reads as a gap and never as a fact. */
const isTBD = (v: string) => v.includes(TBD);

/* Hero film. The still render stays on as the poster, so the first paint is
   instant and reduced-motion visitors keep the premium frame. */
const heroVideo = "/images/projects/ishaan.mp4";

const pageTitle = `Ishaan — G+4 Residential, Karanjade · ${brand.name}`;
const pageDesc =
  "Ishaan by Nesting Tree — a G+4 building at Karanjade, Navi Mumbai. Twelve homes, every one of them a 1RK, with no commercial units in the building.";
---

<BaseLayout title={pageTitle} description={pageDesc} image="/images/og/ishaan.jpg">
  <!-- ============================ NAVBAR ============================ -->
  <SiteHeader sections={sections} ctaHref="#enquire" />

  <!-- ============================ HERO ============================ -->
  <section id="top" class="relative h-[100svh] min-h-[680px] w-full overflow-hidden bg-ink">
    <video
      id="hero-video"
      class="hero-img-reveal absolute top-0 left-0 w-full h-full object-cover"
      style={`object-position:${ishaan.heroPosition}`}
      src={heroVideo}
      poster={ishaan.hero}
      autoplay
      muted
      loop
      playsinline
      preload="auto"
      aria-label="Ishaan — residential building by Nesting Tree, Karanjade, Navi Mumbai"></video>
    <!-- top scrim for nav legibility + bottom scrim for copy -->
    <div class="absolute inset-0 bg-gradient-to-b from-ink/45 via-transparent to-transparent"></div>
    <div class="absolute inset-0 bg-gradient-to-t from-ink via-ink/55 to-transparent"></div>
    <div class="absolute inset-0 bg-gradient-to-r from-ink/45 via-transparent to-transparent"></div>

    <div
      class="relative h-full max-w-[1600px] mx-auto px-5 sm:px-6 lg:px-12 flex flex-col justify-end pb-16 lg:pb-24"
    >
      <!-- breadcrumb -->
      <nav
        class="hero-rise font-display text-[11px] sm:text-xs font-bold uppercase tracking-[0.18em] text-white/65 mb-7"
        style="--rise-d:0.15s"
      >
        <a href="/projects" class="hover:text-white transition-colors">Projects</a>
        <span class="mx-2.5 text-white/35">/</span>
        <span class="text-olive-100">Ishaan</span>
      </nav>

      <!-- status + rera pills -->
      <div class="hero-rise flex flex-wrap items-center gap-2.5 mb-7" style="--rise-d:0.28s">
        <span
          class="inline-flex items-center gap-2 font-display text-[11px] font-bold uppercase tracking-[0.14em] bg-olive text-white px-3.5 py-1.5 rounded-[2px]"
        >
          <span class="w-1.5 h-1.5 rounded-full bg-olive-100"></span>
          {ishaan.status}
        </span>
        {
          ishaan.pills.map((p) => (
            <span class="font-display text-[11px] font-bold uppercase tracking-[0.14em] bg-white/12 text-white border border-white/25 px-3.5 py-1.5 rounded-[2px] backdrop-blur-sm">
              {p}
            </span>
          ))
        }
        {
          /* The MahaRERA pill appears once the number is supplied. */
          !isTBD(ishaan.maharera) && (
            <span class="font-display text-[11px] font-bold uppercase tracking-[0.14em] bg-white/12 text-white border border-white/25 px-3.5 py-1.5 rounded-[2px] backdrop-blur-sm">
              MahaRERA {ishaan.maharera}
            </span>
          )
        }
      </div>

      <h1
        class="hero-rise font-display font-black text-white text-[3.5rem] sm:text-8xl lg:text-[8.5rem] leading-[0.9] tracking-tight"
        style="--rise-d:0.4s"
      >
        Ishaan
      </h1>
      <p
        class="hero-rise text-white/80 text-lg sm:text-xl lg:text-2xl mt-7 max-w-2xl leading-relaxed"
        style="--rise-d:0.54s"
      >
        {ishaan.tagline}
      </p>

      <p
        class="hero-rise flex items-start gap-2.5 text-white/65 text-sm sm:text-base mt-8 max-w-xl leading-relaxed"
        style="--rise-d:0.66s"
      >
        <svg
          class="w-4.5 h-4.5 mt-0.5 shrink-0 text-olive-100"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="1.6"
          stroke-linecap="round"
          stroke-linejoin="round"
          set:html={pin}
        ></svg>
        {ishaan.address}
      </p>
    </div>
  </section>

  <!-- ====================== HEADLINE STATS BAND ====================== -->
  <section class="bg-ink text-stone border-t border-white/10">
    <div class="max-w-[1600px] mx-auto px-5 sm:px-6 lg:px-12">
      <div class="grid grid-cols-2 lg:grid-cols-4">
        {
          heroStats.map((s, i) => (
            <div
              class={`reveal py-12 lg:py-16 px-2 sm:px-4 lg:px-8 ${
                i % 2 === 1 ? "border-l border-white/10" : ""
              } ${i >= 2 ? "border-t border-white/10 lg:border-t-0" : ""} ${
                i !== 0 ? "lg:border-l lg:border-white/10" : ""
              }`}
              style={`--reveal-d:${(i * 0.08).toFixed(2)}s`}
            >
              <p class="font-display font-black text-5xl sm:text-6xl lg:text-7xl tracking-tight text-white leading-none">
                {s.figure}
              </p>
              <p class="font-display text-xs sm:text-sm font-bold uppercase tracking-[0.16em] text-olive-100 mt-4">
                {s.label}
              </p>
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <!-- ============================ OVERVIEW ============================ -->
  <section id="overview" class="bg-stone py-24 lg:py-32 scroll-mt-20">
    <div class="max-w-[1600px] mx-auto px-5 sm:px-6 lg:px-12">
      <div class="grid lg:grid-cols-12 gap-12 lg:gap-20">
        <!-- main column -->
        <div class="lg:col-span-8">
          <span class="reveal font-display text-xs font-bold uppercase tracking-[0.22em] text-olive-600"
            >Project overview</span
          >
          <h2
            class="reveal font-display font-black text-4xl sm:text-5xl lg:text-6xl tracking-tight mt-4 leading-[1.05] max-w-2xl"
            style="--reveal-d:0.08s"
          >
            Twelve homes, one kind
          </h2>
          <p
            class="reveal text-muted text-lg sm:text-xl leading-relaxed mt-7 max-w-2xl"
            style="--reveal-d:0.16s"
          >
            {overview.intro}
          </p>

          <!-- key facts -->
          <dl class="reveal mt-12 border-t border-line">
            {
              overview.facts.map((f) => (
                <div class="grid sm:grid-cols-3 gap-2 sm:gap-8 py-6 border-b border-line">
                  <dt class="font-display text-xs font-bold uppercase tracking-[0.14em] text-muted pt-1">
                    {f.label}
                  </dt>
                  <dd
                    class={
                      isTBD(f.value)
                        ? "sm:col-span-2 text-mutedlight text-base italic pt-1"
                        : "sm:col-span-2 font-display font-semibold text-lg lg:text-xl tracking-tight text-ink"
                    }
                  >
                    {f.value}
                  </dd>
                </div>
              ))
            }
          </dl>

          <!-- unit mix — three tiles; the mix really is one line -->
          <div class="reveal mt-14">
            <span class="font-display text-xs font-bold uppercase tracking-[0.2em] text-olive-600"
              >Unit mix</span
            >
            <div class="grid grid-cols-2 lg:grid-cols-3 gap-px bg-line border border-line mt-5">
              {
                overview.unitMix.map((u) => (
                  <div class="bg-card p-6 lg:p-8">
                    <p class="font-display font-black text-4xl lg:text-5xl tracking-tight text-ink leading-none">
                      {u.figure}
                    </p>
                    <p class="text-muted text-sm leading-snug mt-3">{u.label}</p>
                  </div>
                ))
              }
            </div>
          </div>

          <!-- location & connectivity -->
          <div class="reveal mt-14 bg-olive-50 border border-olive-100 rounded-[3px] p-8 lg:p-10">
            <span class="font-display text-xs font-bold uppercase tracking-[0.2em] text-olive-600"
              >Location & connectivity</span
            >
            <p class="text-inksoft text-lg leading-relaxed mt-4 max-w-2xl">
              {overview.connectivity}
            </p>
            <a
              href={ishaan.mapsUrl}
              target="_blank"
              rel="noopener"
              class="group inline-flex items-center gap-2 font-display font-bold uppercase tracking-[0.06em] text-sm text-olive-600 mt-7"
            >
              <svg
                class="w-4 h-4"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="1.7"
                stroke-linecap="round"
                stroke-linejoin="round"
                set:html={pin}
              ></svg>
              View on Google Maps
              <svg
                class="w-4 h-4 transition-transform group-hover:translate-x-1"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
                set:html={arrow}
              ></svg>
            </a>
          </div>
        </div>

        <!-- sticky enquiry rail -->
        <aside class="lg:col-span-4">
          <div class="lg:sticky lg:top-28">
            <div class="reveal reveal-right bg-ink text-stone rounded-[4px] p-8 lg:p-9">
              <span class="font-display text-xs font-bold uppercase tracking-[0.2em] text-olive-100"
                >Register your interest</span
              >
              <p class="font-display font-black text-2xl lg:text-3xl tracking-tight mt-3 leading-tight">
                Talk to the Ishaan sales team.
              </p>
              <p class="text-stone/60 text-base leading-relaxed mt-4">
                Ishaan is pre-launch. Register early for pricing and first access when bookings open.
              </p>

              <a
                href={sales.phoneHref}
                class="flex items-center gap-4 mt-8 pt-7 border-t border-white/10 group"
              >
                <span
                  class="w-11 h-11 rounded-full bg-olive/25 border border-olive-100/30 flex items-center justify-center shrink-0 text-olive-100"
                >
                  <svg
                    class="w-5 h-5"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="1.6"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    ><path
                      d="M4 4h4l2 5-2.5 1.5a11 11 0 0 0 5 5L14 13l5 2v4a2 2 0 0 1-2 2C9.5 21 3 14.5 3 6a2 2 0 0 1 1-2Z"
                    ></path></svg
                  >
                </span>
                <span>
                  <span
                    class="block font-display text-[11px] font-bold uppercase tracking-[0.16em] text-stone/50"
                    >{sales.name} · Sales</span
                  >
                  <span
                    class="block font-display font-bold text-xl tracking-tight group-hover:text-olive-100 transition-colors"
                    >{sales.phone}</span
                  >
                </span>
              </a>

              <a
                href="#enquire"
                class="block text-center font-display font-bold uppercase tracking-[0.06em] text-sm bg-white text-ink px-6 py-4 rounded-[2px] mt-8 hover:bg-stone transition-colors"
                >Enquire about Ishaan</a
              >
            </div>
          </div>
        </aside>
      </div>
    </div>
  </section>

  <!-- ===================== UNIT TYPES — THE FLAT LAYOUTS ===================== -->
  <section id="units" class="bg-card py-24 lg:py-32 border-t border-line scroll-mt-20">
    <div class="max-w-[1600px] mx-auto px-5 sm:px-6 lg:px-12">
      <div class="reveal max-w-3xl mb-16 lg:mb-24">
        <span class="font-display text-xs font-bold uppercase tracking-[0.22em] text-olive-600"
          >Flat layout</span
        >
        <h2
          class="font-display font-black text-4xl sm:text-5xl lg:text-6xl tracking-tight mt-4 leading-[1.05]"
        >
          One kind of home to call Ishaan.
        </h2>
        <p class="text-muted text-lg sm:text-xl leading-relaxed mt-6">
          Twelve 1RK homes, four to a floor, and all of them a 1RK.
        </p>
      </div>

      <div class="flex flex-col gap-24 lg:gap-32">
        {
          unitTypes.map((u, i) => (
            <article class="reveal grid lg:grid-cols-2 gap-12 lg:gap-20 items-center">
              {/* flat layout render */}
              <div class={`relative ${i % 2 === 1 ? "lg:order-2" : ""}`}>
                <div class="absolute -inset-3 sm:-inset-5 bg-olive-50 rounded-[4px] -z-0" />
                <div class="relative bg-stone border border-line rounded-[3px] p-4 sm:p-6 lg:p-8">
                  <img
                    src={u.plan}
                    alt={
                      u.planPending
                        ? `Ishaan ${u.type} — layout drawing not yet available`
                        : `Ishaan ${u.type} flat layout`
                    }
                    class={`w-full h-auto object-contain rounded-[2px] ${
                      u.planPending ? "opacity-60" : "mix-blend-multiply"
                    }`}
                    loading="lazy"
                  />
                  <span
                    class={`absolute top-7 left-7 font-display text-[11px] font-bold uppercase tracking-[0.14em] px-3 py-1.5 rounded-[2px] ${
                      u.planPending
                        ? "bg-stone text-muted border border-dashed border-line"
                        : "bg-ink text-stone"
                    }`}
                  >
                    {u.planPending ? "Layout pending" : "Flat layout"}
                  </span>
                  {u.planPending && (
                    <p class="text-mutedlight text-xs leading-relaxed mt-4 italic">
                      Placeholder image — the {u.type} layout drawing has not been supplied yet.
                    </p>
                  )}
                </div>
              </div>

              {/* details */}
              <div class={i % 2 === 1 ? "lg:order-1" : ""}>
                <div class="flex flex-wrap items-center gap-3">
                  <h3 class="font-display font-black text-4xl lg:text-5xl tracking-tight">
                    {u.type}
                  </h3>
                  <span class="font-display text-xs font-bold uppercase tracking-[0.12em] bg-olive-100 text-olive-dark px-3 py-1.5 rounded-full">
                    {u.units}
                  </span>
                </div>
                <p
                  class={
                    isTBD(u.series)
                      ? "text-mutedlight text-sm italic mt-4"
                      : "font-display text-sm font-bold uppercase tracking-[0.16em] text-olive-600 mt-4"
                  }
                >
                  {isTBD(u.series) ? `Series — ${TBD}` : u.series}
                </p>
                <p class="text-muted text-lg leading-relaxed mt-6 max-w-md">{u.blurb}</p>

                {/* RERA carpet figures from the approved plan */}
                <AreaStats stats={u.stats} heading="Areas — approved plan" class="mt-9 max-w-md" />

                {/* key features — hidden while empty rather than faked */}
                {u.features.length > 0 && (
                  <ul class="mt-7 flex flex-col gap-3 max-w-md">
                    {u.features.map((f) => (
                      <li class="flex items-start gap-3 text-ink">
                        <svg
                          class="w-5 h-5 mt-0.5 shrink-0 text-olive-600"
                          viewBox="0 0 24 24"
                          fill="none"
                          stroke="currentColor"
                          stroke-width="1.8"
                          stroke-linecap="round"
                          stroke-linejoin="round"
                          set:html={check}
                        />
                        <span class="text-base leading-snug">{f}</span>
                      </li>
                    ))}
                  </ul>
                )}

                {/* room dimensions */}
                <div class="mt-9 border-t border-line max-w-md">
                  <p class="font-display text-xs font-bold uppercase tracking-[0.16em] text-muted pt-5 pb-1">
                    Room dimensions
                  </p>
                  {u.rooms.length > 0 ? (
                    <dl>
                      {u.rooms.map((r) => (
                        <div class="flex items-baseline justify-between gap-4 py-3 border-b border-line">
                          <dt class="text-muted text-base">{r.room}</dt>
                          <dd class="font-display font-semibold tracking-tight text-ink text-base tabular-nums">
                            {r.dim}
                          </dd>
                        </div>
                      ))}
                    </dl>
                  ) : (
                    <p class="text-mutedlight text-base italic py-3">{TBD}</p>
                  )}
                </div>

                {u.features.length === 0 && (
                  <p class="text-mutedlight text-sm italic mt-5 max-w-md">
                    Key features for the {u.type} — {TBD}
                  </p>
                )}
              </div>
            </article>
          ))
        }
      </div>

      {
        features.areaStats && (
          <p class="reveal text-mutedlight text-xs leading-relaxed mt-16 lg:mt-20 max-w-3xl">
            {unitTypesNote}
          </p>
        )
      }
    </div>
  </section>

  <!-- ===================== CONSTRUCTION PROGRESS ===================== -->
  <BuildProgress
    steps={buildProgress}
    heading="Structure done. Finishes under way."
    intro="All the RCC work is complete, and finishing (tiling, painting and plaster) is running across the twelve homes. Here is exactly where Ishaan stands on the way to your keys, with full readiness targeted for March 2027."
    sectionClass="bg-stone py-24 lg:py-32 border-t border-line scroll-mt-20"
  />

  <!-- ============================ FLOOR PLANS ============================ -->
  <section id="floorplans" class="bg-stone py-24 lg:py-32 border-t border-line scroll-mt-20">
    <div class="max-w-[1600px] mx-auto px-5 sm:px-6 lg:px-12">
      <div class="reveal max-w-3xl mb-16 lg:mb-24">
        <span class="font-display text-xs font-bold uppercase tracking-[0.22em] text-olive-600"
          >Floor plans</span
        >
        <h2
          class="font-display font-black text-4xl sm:text-5xl lg:text-6xl tracking-tight mt-4 leading-[1.05]"
        >
          How Ishaan is laid out.
        </h2>
        <p class="text-muted text-lg sm:text-xl leading-relaxed mt-6">
          Twelve homes stacked above a ground floor kept for the building itself.
        </p>
      </div>

      <div class="flex flex-col gap-24 lg:gap-32">
        {
          floorPlans.map((fp, i) => (
            <article class="reveal grid lg:grid-cols-2 gap-12 lg:gap-20 items-center">
              {/* floor-plan render */}
              <div class={`relative ${i % 2 === 1 ? "lg:order-2" : ""}`}>
                <div class="absolute -inset-3 sm:-inset-5 bg-olive-50 rounded-[4px] -z-0" />
                <div class="relative bg-stone border border-line rounded-[3px] p-4 sm:p-6 lg:p-8">
                  <img
                    src={fp.plan}
                    alt={
                      fp.planPending
                        ? `Ishaan ${fp.title} — plan drawing not yet available`
                        : `Ishaan ${fp.title} plan — ${fp.label}`
                    }
                    class={`w-full h-auto object-contain rounded-[2px] ${
                      fp.planPending ? "opacity-60" : "mix-blend-multiply"
                    }`}
                    loading="lazy"
                  />
                  <span
                    class={`absolute top-7 left-7 font-display text-[11px] font-bold uppercase tracking-[0.14em] px-3 py-1.5 rounded-[2px] ${
                      fp.planPending
                        ? "bg-stone text-muted border border-dashed border-line"
                        : "bg-ink text-stone"
                    }`}
                  >
                    {fp.planPending ? "Plan pending" : "Floor plan"}
                  </span>
                  {fp.planPending && (
                    <p class="text-mutedlight text-xs leading-relaxed mt-4 italic">
                      Placeholder image — the {fp.title.toLowerCase()} drawing has not been supplied
                      yet.
                    </p>
                  )}
                </div>
              </div>

              {/* details */}
              <div class={i % 2 === 1 ? "lg:order-1" : ""}>
                <h3 class="font-display font-black text-4xl lg:text-5xl tracking-tight">
                  {fp.title}
                </h3>
                <p class="font-display text-sm font-bold uppercase tracking-[0.16em] text-olive-600 mt-4">
                  {fp.label}
                </p>
                <p class="text-muted text-lg leading-relaxed mt-6 max-w-md">{fp.blurb}</p>

                <ul class="mt-8 grid sm:grid-cols-2 gap-x-8 gap-y-3.5 max-w-lg">
                  {fp.points.map((pt) => (
                    <li
                      class={`flex items-start gap-3 ${isTBD(pt) ? "text-mutedlight" : "text-ink"}`}
                    >
                      <svg
                        class={`w-5 h-5 mt-0.5 shrink-0 ${
                          isTBD(pt) ? "text-mutedlight" : "text-olive-600"
                        }`}
                        viewBox="0 0 24 24"
                        fill="none"
                        stroke="currentColor"
                        stroke-width="1.8"
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        set:html={isTBD(pt) ? `<path d="M5 12h14"/>` : check}
                      />
                      <span class={`text-base leading-snug ${isTBD(pt) ? "italic" : ""}`}>{pt}</span>
                    </li>
                  ))}
                </ul>
              </div>
            </article>
          ))
        }
      </div>
    </div>
  </section>

  <!-- ===================== AMENITIES — "ISHAAN KEEPS IT CLEAR" =====================
       Section backgrounds alternate stone → card → stone → card down the
       page, matching shikhar.astro (the other now-booking page). The level
       cards below invert to bg-stone so they read against this one. -->
  <section id="amenities" class="bg-card scroll-mt-20">
    {/* Section intro — the north-east framing. This reads the NAME,
        not a claim about orientation or Vastu compliance. */}
    <div class="max-w-[1600px] mx-auto px-5 sm:px-6 lg:px-12 pt-24 lg:pt-32">
      <div class="reveal max-w-3xl">
        <span class="font-display text-xs font-bold uppercase tracking-[0.22em] text-olive-600"
          >The corner you keep clear</span
        >
        <h2
          class="font-display font-black text-4xl sm:text-5xl lg:text-6xl tracking-tight mt-4 leading-[1.05]"
        >
          Ishaan keeps it clear.
        </h2>
        <p class="text-muted text-lg sm:text-xl leading-relaxed mt-6 max-w-2xl">
          Ishaan is the name of the north-east: in Vastu, the corner of first light, the one you
          leave open and uncluttered. The building is built the same way: four floors, twelve
          homes, one kind of home, and nothing else competing for the room.
        </p>
      </div>
    </div>

    {/* ===== The three readings: use, count, plan ===== */}
    <div class="max-w-[1600px] mx-auto px-5 sm:px-6 lg:px-12 mt-14 lg:mt-20">
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6 lg:gap-7">
        {
          levels.map((l, i) => (
            <article
              class="reveal flex flex-col bg-stone border border-line rounded-[4px] p-7 lg:p-8 hover:border-olive-100 transition-colors"
              style={`--reveal-d:${(i * 0.06).toFixed(2)}s`}
            >
              <div class="flex items-start justify-between gap-4">
                <span class="flex items-center justify-center w-12 h-12 rounded-[3px] bg-olive-50 text-olive-600 shrink-0">
                  <svg
                    class="w-6 h-6"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="1.5"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    set:html={icons[l.icon]}
                  />
                </span>
                <div class="flex flex-col items-end gap-2">
                  <span class="font-display font-black text-2xl tracking-tight text-olive-600/80 tabular-nums leading-none">
                    {l.marker}
                  </span>
                  {l.featured && (
                    <span class="font-display text-[9px] font-bold uppercase tracking-[0.16em] bg-olive-100 text-olive-dark px-2 py-0.5 rounded-full">
                      Signature
                    </span>
                  )}
                </div>
              </div>

              <h3 class="font-display font-black text-xl lg:text-2xl tracking-tight mt-6 leading-tight">
                {l.title}
              </h3>
              <p class="text-muted text-base leading-relaxed mt-3">{l.blurb}</p>

              <ul class="mt-6 pt-6 border-t border-line flex flex-col gap-3">
                {l.points.map((p) => (
                  <li class="flex items-start gap-2.5 text-ink">
                    <svg
                      class="w-4 h-4 mt-1 shrink-0 text-olive-600"
                      viewBox="0 0 24 24"
                      fill="none"
                      stroke="currentColor"
                      stroke-width="2"
                      stroke-linecap="round"
                      stroke-linejoin="round"
                      aria-hidden="true"
                      set:html={check}
                    />
                    <span class="text-[15px] leading-snug">{p}</span>
                  </li>
                ))}
              </ul>
            </article>
          ))
        }
      </div>
    </div>

    {/* ===== Full amenity list — renders as soon as `amenityGroups` is filled ===== */}
    <div
      id="amenities-all"
      class="max-w-[1600px] mx-auto px-5 sm:px-6 lg:px-12 py-24 lg:py-32 mt-24 lg:mt-32 border-t border-line scroll-mt-20"
    >
      <div class="reveal max-w-3xl mb-14 lg:mb-16">
        <span class="font-display text-xs font-bold uppercase tracking-[0.22em] text-olive-600"
          >The full spec</span
        >
        <h3
          class="font-display font-black text-3xl sm:text-4xl lg:text-5xl tracking-tight mt-4 leading-[1.05]"
        >
          Every amenity, in full.
        </h3>
      </div>

      {
        amenityGroups.length > 0 ? (
          <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-x-10 gap-y-12">
            {amenityGroups.map((g, gi) => (
              <div class="reveal" style={`--reveal-d:${(gi * 0.08).toFixed(2)}s`}>
                <h4 class="font-display font-black text-xl tracking-tight border-b border-line pb-3 mb-5">
                  {g.group}
                </h4>
                <ul class="flex flex-col gap-4">
                  {g.items.map((a) => (
                    <li class="flex items-start gap-3">
                      <span class="flex items-center justify-center w-8 h-8 rounded-[2px] bg-olive-50 text-olive-600 shrink-0">
                        <svg
                          class="w-4 h-4"
                          viewBox="0 0 24 24"
                          fill="none"
                          stroke="currentColor"
                          stroke-width="1.6"
                          stroke-linecap="round"
                          stroke-linejoin="round"
                          set:html={icons[a.icon]}
                        />
                      </span>
                      <p class="font-display font-semibold text-sm tracking-tight leading-snug pt-1">
                        {a.name}
                        {a.featured && (
                          <span class="ml-2 align-middle font-display text-[9px] font-bold uppercase tracking-[0.16em] bg-olive-100 text-olive-dark px-1.5 py-0.5 rounded-full">
                            Signature
                          </span>
                        )}
                      </p>
                    </li>
                  ))}
                </ul>
              </div>
            ))}
          </div>
        ) : (
          /* No amenity schedule supplied — say so plainly rather than
             invent one. Fill `amenityGroups` in src/data/ishaan.ts and
             this panel is replaced by the grouped list above. */
          <div class="reveal border border-dashed border-line rounded-[4px] p-8 lg:p-12 max-w-3xl">
            <p class="font-display font-bold text-lg lg:text-xl tracking-tight text-ink">
              {TBD}
            </p>
            <p class="text-muted text-base leading-relaxed mt-3">
              The amenity schedule for Ishaan has not been supplied. Rather than borrow another
              project's list, this block stays empty until the real one arrives. Then it prints
              here grouped as Building, Lifestyle and Interiors, the same as every other project
              page.
            </p>
            <p class="font-display text-xs font-bold uppercase tracking-[0.16em] text-muted mt-8 pt-6 border-t border-line">
              Still needed
            </p>
            <ul class="grid sm:grid-cols-2 gap-x-8 gap-y-2.5 mt-4">
              {amenitiesNeeded.map((n) => (
                <li class="flex items-start gap-3 text-mutedlight">
                  <svg
                    class="w-4 h-4 mt-1 shrink-0"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="1.8"
                    stroke-linecap="round"
                    aria-hidden="true"
                  >
                    <path d="M5 12h14" />
                  </svg>
                  <span class="text-[15px] leading-snug">{n}</span>
                </li>
              ))}
            </ul>
          </div>
        )
      }
    </div>
  </section>

  <!-- ===================== ABOUT THE DEVELOPER (DARK) ===================== -->
  <section id="developer" class="bg-ink text-stone py-24 lg:py-32 scroll-mt-20">
    <div class="max-w-[1600px] mx-auto px-5 sm:px-6 lg:px-12">
      <div class="grid lg:grid-cols-12 gap-12 lg:gap-20">
        <div class="reveal reveal-left lg:col-span-5">
          <span class="font-display text-xs font-bold uppercase tracking-[0.22em] text-olive-100"
            >About the developer</span
          >
          <h2
            class="font-display font-black text-4xl sm:text-5xl lg:text-6xl tracking-tight mt-4 leading-[1.05]"
          >
            Built by Nesting Tree.
          </h2>
          <p class="text-stone/60 text-lg sm:text-xl leading-relaxed mt-6 max-w-md">
            Ishaan is one of Nesting Tree's residences in Karanjade, drawing on two decades of Navi Mumbai real estate.
          </p>
        </div>

        <div class="lg:col-span-7 grid sm:grid-cols-2 gap-5 lg:gap-7">
          {
            developerNotes.map((n, i) => (
              <div
                class="reveal bg-white/[0.04] border border-white/10 rounded-[4px] p-8 lg:p-9 hover:border-olive-100/40 transition-colors"
                style={`--reveal-d:${(i * 0.08).toFixed(2)}s`}
              >
                <span class="font-display text-xs font-bold uppercase tracking-[0.16em] text-olive-100">
                  {n.label}
                </span>
                <p class="text-stone/70 text-base lg:text-lg leading-relaxed mt-4">{n.body}</p>
              </div>
            ))
          }
        </div>
      </div>
    </div>
  </section>

  <!-- ===================== WHY ISHAAN ===================== -->
  <section class="bg-olive-50 py-24 lg:py-32">
    <div class="max-w-[1600px] mx-auto px-5 sm:px-6 lg:px-12">
      <div class="reveal max-w-3xl">
        <span class="font-display text-xs font-bold uppercase tracking-[0.22em] text-olive-600"
          >Why Ishaan</span
        >
        <h2
          class="font-display font-black text-4xl sm:text-5xl lg:text-6xl tracking-tight mt-4 leading-[1.05]"
        >
          The Nesting Tree standard.
        </h2>
        <p class="text-muted text-lg sm:text-xl leading-relaxed mt-6">
          Building homes with more amenities in Navi Mumbai since {
            brand.legacyYear
          }.
        </p>
      </div>

      <div
        class="grid sm:grid-cols-2 lg:grid-cols-3 gap-px bg-olive-100 border border-olive-100 mt-14 lg:mt-16"
      >
        {
          whyIshaan.map((r, i) => (
            <div class="reveal bg-olive-50 p-8 lg:p-10" style={`--reveal-d:${(i * 0.07).toFixed(2)}s`}>
              <span class="font-display font-black text-3xl text-olive/45 leading-none">
                {String(i + 1).padStart(2, "0")}
              </span>
              <h3 class="font-display font-bold text-xl lg:text-2xl tracking-tight mt-5">
                {r.title}
              </h3>
              <p class="text-muted text-base leading-relaxed mt-3">{r.body}</p>
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <!-- ===================== GALLERY ===================== -->
  <ProjectGallery
    project="Ishaan"
    items={gallery}
    lead="Real photographs from the Ishaan site: the residence and its interiors, shot on site during construction."
  />

  <!-- ===================== QR CODE ===================== -->
  <QrBlock project="Ishaan" />

  <!-- ===================== ENQUIRY FORM ===================== -->
  <section id="enquire" class="bg-stone py-24 lg:py-32 border-t border-line scroll-mt-20">
    <div class="reveal max-w-2xl mx-auto px-5 sm:px-6 text-center">
      <span class="font-display text-xs font-bold uppercase tracking-[0.22em] text-olive-600"
        >Enquire</span
      >
      <h2
        class="font-display font-black text-4xl sm:text-5xl lg:text-6xl tracking-tight mt-4 leading-[1.05]"
      >
        Ask about Ishaan.
      </h2>
      <p class="text-muted text-lg leading-relaxed mt-6 max-w-xl mx-auto">
        Share your details and our team will get back to you with availability and pricing. Or call{" "}
        {sales.name} on{" "}
        <a
          href={sales.phoneHref}
          class="font-semibold text-olive-600 hover:text-olive-dark transition-colors">{sales.phone}</a
        >.
      </p>

      <form class="mt-12 text-left" action="/api/contact" method="POST">
        <div class="grid sm:grid-cols-2 gap-5">
          <div class="flex flex-col gap-2">
            <label
              for="name"
              class="font-display text-xs font-bold uppercase tracking-[0.14em] text-muted"
              >Full name</label
            >
            <input
              id="name"
              name="name"
              type="text"
              required
              placeholder="Your name"
              class="bg-card border border-line rounded-[2px] px-4 py-3.5 text-ink placeholder:text-mutedlight focus:outline-none focus:border-olive focus:ring-2 focus:ring-olive/20 transition"
            />
          </div>
          <div class="flex flex-col gap-2">
            <label
              for="phone"
              class="font-display text-xs font-bold uppercase tracking-[0.14em] text-muted"
              >Phone</label
            >
            <input
              id="phone"
              name="phone"
              type="tel"
              required
              placeholder="Mobile number"
              class="bg-card border border-line rounded-[2px] px-4 py-3.5 text-ink placeholder:text-mutedlight focus:outline-none focus:border-olive focus:ring-2 focus:ring-olive/20 transition"
            />
          </div>
          <div class="flex flex-col gap-2">
            <label
              for="email"
              class="font-display text-xs font-bold uppercase tracking-[0.14em] text-muted"
              >Email</label
            >
            <input
              id="email"
              name="email"
              type="email"
              placeholder="you@example.com"
              class="bg-card border border-line rounded-[2px] px-4 py-3.5 text-ink placeholder:text-mutedlight focus:outline-none focus:border-olive focus:ring-2 focus:ring-olive/20 transition"
            />
          </div>
          <div class="flex flex-col gap-2">
            <label
              for="project"
              class="font-display text-xs font-bold uppercase tracking-[0.14em] text-muted"
              >Project</label
            >
            <select
              id="project"
              name="project"
              class="bg-card border border-line rounded-[2px] px-4 py-3.5 text-ink focus:outline-none focus:border-olive focus:ring-2 focus:ring-olive/20 transition appearance-none"
            >
              {
                projects.map((p) => (
                  <option value={p.slug} selected={p.slug === "ishaan"}>
                    {p.name}
                  </option>
                ))
              }
            </select>
          </div>
        </div>

        <div class="flex flex-col gap-2 mt-5">
          <label
            for="message"
            class="font-display text-xs font-bold uppercase tracking-[0.14em] text-muted"
            >Message <span class="font-medium normal-case tracking-normal text-mutedlight"
              >(optional)</span
            ></label
          >
          <textarea
            id="message"
            name="message"
            rows="4"
            placeholder="When are you looking to move in?"
            class="bg-card border border-line rounded-[2px] px-4 py-3.5 text-ink placeholder:text-mutedlight focus:outline-none focus:border-olive focus:ring-2 focus:ring-olive/20 transition resize-y"
          ></textarea>
        </div>

        <button
          type="submit"
          class="w-full mt-7 font-display font-bold uppercase tracking-[0.06em] text-sm bg-ink text-stone px-7 py-4 rounded-[2px] hover:bg-inksoft transition-colors"
          >Send enquiry</button
        >
        <p class="text-mutedlight text-xs leading-relaxed mt-4 text-center">
          By submitting, you agree to be contacted about Ishaan by Nesting Tree.
        </p>
      </form>
    </div>
  </section>

  <!-- ===================== FOOTER ===================== -->
  <footer class="bg-ink text-stone">
    <div class="max-w-[1600px] mx-auto px-5 sm:px-6 lg:px-12 pt-16 pb-10 border-b border-white/10">
      <h3
        class="reveal font-display font-black text-[3.25rem] sm:text-[6rem] lg:text-[8rem] leading-none tracking-tight"
      >
        NESTING TREE
      </h3>
    </div>

    <div class="max-w-[1600px] mx-auto px-5 sm:px-6 lg:px-12 py-14">
      <div class="grid sm:grid-cols-2 lg:grid-cols-4 gap-12">
        <div class="reveal">
          <p class="text-stone/55 text-sm leading-relaxed max-w-[240px]">
            Residential developer in Navi Mumbai, building homes with more amenities
            since {brand.legacyYear}.
          </p>
          <div class="flex items-center gap-3 mt-6">
            {
              contact.socials.map((s) => (
                <a
                  href={s.href}
                  class="w-9 h-9 rounded-[2px] bg-white/5 border border-white/10 flex items-center justify-center text-xs font-display font-bold hover:bg-white/10 transition-colors"
                >
                  {s.label}
                </a>
              ))
            }
          </div>
        </div>

        <div class="reveal" style="--reveal-d:0.08s">
          <h5 class="font-display font-bold text-sm uppercase tracking-wide text-stone/40 mb-4">
            This project
          </h5>
          <ul class="space-y-3 text-sm font-medium">
            {
              sections.map((item) => (
                <li>
                  <a href={item.href} class="hover:text-olive-100 transition-colors">
                    {item.label}
                  </a>
                </li>
              ))
            }
          </ul>
        </div>

        <div class="reveal" style="--reveal-d:0.16s">
          <h5 class="font-display font-bold text-sm uppercase tracking-wide text-stone/40 mb-4">
            Explore
          </h5>
          <ul class="space-y-3 text-sm font-medium text-stone/80">
            <li><a href="/" class="hover:text-olive-100 transition-colors">Home</a></li>
            <li>
              <a href="/projects" class="hover:text-olive-100 transition-colors">All projects</a>
            </li>
            <li>
              <a href="/#journey" class="hover:text-olive-100 transition-colors">Our journey</a>
            </li>
            <li><a href="#enquire" class="hover:text-olive-100 transition-colors">Enquire</a></li>
          </ul>
        </div>

        <div class="reveal" style="--reveal-d:0.24s">
          <h5 class="font-display font-bold text-sm uppercase tracking-wide text-stone/40 mb-4">
            Ishaan sales
          </h5>
          <ul class="space-y-3 text-sm font-medium text-stone/80">
            <li class="flex items-start gap-2.5">
              <svg
                class="w-4 h-4 mt-0.5 shrink-0 text-olive-100"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="1.6"
                set:html={pin}
              ></svg>
              {ishaan.address}
            </li>
            <li class="flex items-center gap-2.5">
              <svg
                class="w-4 h-4 shrink-0 text-olive-100"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="1.6"
                ><path
                  d="M4 4h4l2 5-2.5 1.5a11 11 0 0 0 5 5L14 13l5 2v4a2 2 0 0 1-2 2C9.5 21 3 14.5 3 6a2 2 0 0 1 1-2Z"
                ></path></svg
              >
              <a href={sales.phoneHref} class="hover:text-olive-100 transition-colors"
                >{sales.name} — {sales.phone}</a
              >
            </li>
            <li class="text-stone/55">
              {ishaan.approvals}
            </li>
          </ul>
        </div>
      </div>

      <div
        class="border-t border-white/10 mt-12 pt-6 flex flex-col sm:flex-row items-center justify-between gap-4 text-xs text-stone/40"
      >
        <p>&copy; {new Date().getFullYear()} {brand.name}. All rights reserved.</p>
        <p>{contact.maharera}</p>
        <div class="flex items-center gap-5">
          <a href="/" class="hover:text-stone/70 transition-colors">Back to home</a>
        </div>
      </div>
    </div>
  </footer>

  <script is:inline>
    // Hero film holds on its poster frame for reduced-motion visitors.
    if (window.matchMedia("(prefers-reduced-motion: reduce)").matches) {
      const heroVideo = document.getElementById("hero-video");
      if (heroVideo) {
        heroVideo.removeAttribute("autoplay");
        heroVideo.pause();
      }
    }

    // Scroll-reveal: fade/lift elements in as they enter the viewport.
    // Only wired up when the head guard enabled reveal-on (JS + motion OK).
    if (document.documentElement.classList.contains("reveal-on")) {
      const io = new IntersectionObserver(
        (entries, obs) => {
          for (const entry of entries) {
            if (entry.isIntersecting) {
              entry.target.classList.add("in-view");
              obs.unobserve(entry.target);
            }
          }
        },
        { rootMargin: "0px 0px -10% 0px", threshold: 0.1 }
      );
      document.querySelectorAll(".reveal, .reveal-img").forEach((el) => io.observe(el));
    }
  </script>
</BaseLayout>